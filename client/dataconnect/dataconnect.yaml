specVersion: "v1beta"
serviceId: "client"
location: "asia-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "client-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
